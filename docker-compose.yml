services:
  license-forge:
    build:
      context: .
      dockerfile: environment/Dockerfile
    image: license-forge:latest
    container_name: license-forge
    ports:
      - "5000:5000"
    network_mode: bridge
    mem_limit: 512m
    cpus: 1.0
