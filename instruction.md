# License Forge Bypass

License Forge Corp runs a license activation server at `nc HOST 5000`.

It hands out free TRIAL licenses without question, but refuses to activate
a FULL license honestly. Find a way to get a FULL license activated anyway,
then collect the reward.

Type `HELP` after connecting to see what the server can do. Submit the flag
you find in the form `flag{...}`.
